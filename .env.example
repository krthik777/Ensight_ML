# .env.example
# Copy this to .env for local development (never commit .env)
#
# Full Atlas URI format — must include /dbName and connection options
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/energydashboard?retryWrites=true&w=majority
MONGO_DB=energydashboard
PORT=5050
